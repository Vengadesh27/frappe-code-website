[build]
  publish = "dist"
  command = "npm install && npm run build"

# SPA history-mode routing: serve index.html for any unknown path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
